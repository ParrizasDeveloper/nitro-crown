import { Anton, Roboto } from 'next/font/google'
export const roboto = Roboto({
    weight: 'variable',
    subsets: ['latin']
})

export const anton = Anton({
    weight: '400',
    subsets: ['latin']
})